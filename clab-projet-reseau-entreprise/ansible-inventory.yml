all:
  vars:
    # The generated inventory is assumed to be used from the clab host.
    # Hence no http proxy should be used. Therefore we make sure the http
    # module does not attempt using any global http proxy.
    ansible_httpapi_use_proxy: false
  children:
    arista_ceos:
      vars:
        # ansible_connection: set ansible_connection variable if required
        ansible_user: admin
        ansible_password: admin
      hosts:
        clab-projet-reseau-entreprise-ENT-COR-01:
          ansible_host: 172.20.20.6
    linux:
      hosts:
        clab-projet-reseau-entreprise-CLIENT-PC:
          ansible_host: 172.20.20.8
        clab-projet-reseau-entreprise-CLIENT-PHONE:
          ansible_host: 172.20.20.10
        clab-projet-reseau-entreprise-ENT-FIREWALL:
          ansible_host: 172.20.20.12
        clab-projet-reseau-entreprise-SRV-DNS:
          ansible_host: 172.20.20.7
        clab-projet-reseau-entreprise-SRV-VOIP:
          ansible_host: 172.20.20.11
        clab-projet-reseau-entreprise-SRV-WEB:
          ansible_host: 172.20.20.9
